# 追加5項目の実行記録（2026-09-10）

依頼: 提示した追加改修5件を実装する。Lunaが小さな単位で実装し、親が仕様・レビュー・統合検証を担当する。
前回の未コミット変更を基準として保持する。HEADは `53897b912b390914dda145c98dd72d8147ab6b0e`、ローカル候補は1.6.0。

| 順 | Task | 内容 | 検収条件 | 状態 |
|---|---|---|---|---|
| 1 | 20260910-07 | CIへmaintenance検証を接続 | PR/Releaseの必須経路でtests・Birdseye・台帳・文書参照を検証 | 対象検証済み。GitHub runner未実行 |
| 2 | 20260910-08 | 実行ID衝突防止 | 同時刻の独立実行を識別でき、agentの冪等性・fingerprintを維持 | 実装・対象検証完了。packageのagent再利用も成功 |
| 3 | 20260910-09 | diff severity policy | 任意policyの調整と終了判定・成果物が一致、無指定時の互換を維持 | 対象69テスト・package検証成功 |
| 4 | 20260910-10 | 精度レビュー画面 | 根拠表示・分類・保存・再開、既存reviewの入力照合とAI/人手区別を維持 | 実装・対象回帰・実ブラウザ・package検証完了 |
| 5 | 20260910-11 | severity理由表示 | Markdown/Viewerで元値・適用値・理由を確認できる | 対象94テスト・実ブラウザ検証完了 |

## 実装方針

- CI設定と実行IDの生成箇所の変更は、ユーザーが承認した追加5件の範囲に限る。governanceの保護を緩めない。
- 実行IDは実行の識別、fingerprintは入力・内容の同一性を担当する。agent requestの再利用契約を変更しない。
- 精度レビュー画面はローカルのデータで動作し、判定JSONを保存して再開できる。AI案を人手判定に自動変換しない。
- 実装ごとに対象テストを実行し、最後にbuild/lint/typecheck/full tests/coverage/package smokeを行う。coverage閾値は80%を維持する。
- Birdseyeは共通文書の最終更新後に再生成し、checkで検証する。
- 人手精度の合格、過去記録2件の確認、公開承認は今回の機能追加では代替しない。

前回検収: `docs/acceptance/AC-20260910-06-maintenance.md`。
今回の検証ログ: `C:/Users/ryo-n/Codex_dev/code-to-gate-work-20260910-next/`。

## 監督レビュー

- CIはcoverage/analyzeの既存必須経路へ接続した。workflowの権限は増やしていない。
- 実workspaceのBirdseye対象にはCRLFのsourceが17件あったため、`.gitattributes`だけに依存する案を差し戻した。generatorのsource読込でLFへ正規化し、CRLF生成→LF照合成功・実内容変更失敗を検証した。maintenance 13 tests成功。
- 実行IDの変更は独立実行を示すruntime生成箇所に限定する。旧pure helper `generateRunId(timestamp)`の出力とagent fingerprint IDは維持する。
- 親の同時計時graph回帰テストで、graphCache hitが旧run IDも複製することを検出した。cacheされた解析内容は維持し、clone後の日時とrun IDを更新する修正で3 files / 10 testsが成功した。architecture 18 testsも成功。
- diffの初回対象50テストは成功したが、親レビューで空raw artifactのcompletenessとYAMLの暗黙読み飛ばしを指摘した。実Git差分の昇降格・件数・audit・exitの回帰検証を追加した。
- diffの再レビューで省略可能なblocking subsectionの誤拒否、colon入りIDの解釈、空/非空のsummary構造を修正した。DB差分への適用とGit失敗経路を含む69テストが成功した。
- 精度UIの実ブラウザ操作で検索不動を検出し修正へ戻した。JSON保存・既存summarize検証・再読込・日本語コメントの復元は成功した。
- 精度UIの対象テストに作業者ローカルのQA directory依存があったため、CIで自立するfixtureへ置き換えた。内蔵browserで未対応のpromptは画面内reviewer入力へ置き換えた。
- 精度UIは親の独立したイベント回帰14 testsと実ブラウザで検証した。別runの読込拒否、AIのままの保存、人手レビュー開始時の初期化、1,449件の29ページの移動を確認した。EOF付近の誤った省略表示も回帰検証とともに修正した。
- severity詳細はpublic profileで元値/適用値だけ表示する。Markdown表とHTML詳細の複数行理由・表示記号を調整した。ViewerのMermaid本文露出とfilter後の件数誤表示も修正し、実ブラウザで確認した。

最終統合のゲート結果は [AC-20260910-12](../docs/acceptance/AC-20260910-12-follow-up.md) に記録する。
