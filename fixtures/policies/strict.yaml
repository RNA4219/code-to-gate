version: ctg/v1alpha1
name: strict
description: Strict synthetic policy for v0.1 fixture acceptance.
blocking:
  severities:
    - critical
  categories:
    - payment
  rules:
    - CLIENT_TRUSTED_PRICE
    - WEAK_AUTH_GUARD
readiness:
  criticalFindingStatus: blocked_input
  highAuthFindingStatus: blocked_input
  defaultRiskStatus: needs_review

