// TypeScript file with syntax error
import { something } from "./somewhere";

// Invalid TypeScript syntax
const broken: invalid = {
  function missing(): string {
    return "this has syntax errors"
  }
};

// Missing closing brace
class BrokenClass {
  constructor() {
    this.value =
    // Missing expression