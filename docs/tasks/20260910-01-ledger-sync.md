---
task_id: 20260910-01
intent_id: DOC-LEDGER-SYNC-001
owner: code-to-gate-team
status: done
last_reviewed_at: 2026-09-10
next_review_due: 2026-10-10
---

# 改修台帳・文書同期

親レビュー完了: 2026-09-10。公開時点・suppression・精度保証を区別する修正を確認。
完了チェックの専用 Node tests 4件も親が再実行して成功した。
受入記録: [AC-20260910-01](../acceptance/AC-20260910-01.md)。

## 背景

既存の改修記録に、実装済みの detector 対策、Markdown の人間レビューガイド、
配布状態、SPEC-30 の記載ずれが残っていた。実 repo 実行結果には人手判定が
ないため、実行成功と精度受入を分けて記録する必要がある。

## ゴール

指定された台帳・プロジェクト文書を、確認できた根拠に合わせて同期する。
完了した対策、レビュー待ちの受入、継続中の残件、過去時点の記録を混同しない。

## 修正対象

- `docs/rule-precision-backlog.md`
- `docs/product-maturity-issues.md`
- `docs/public-readiness/follow-up-backlog.md`
- `docs/specs/SPEC-MASTER.md`
- `CLAUDE.md`
- `docs/TASKS.md`

関連するコード・記録の参照:

- `src/rules/hardcoded-secret.ts`: schema property の detector 除外、self-reference は suppression 継続
- `src/rules/debt-marker.ts`: compatibility/workaround 説明の除外
- `src/rules/raw-sql.ts`: SQL query 条件を伴わない rule 名文字列の除外
- `src/reporters/markdown-reporter.ts`: Human Review Guide と確認コマンド
- `docs/distribution-status.md`: package/GitHub release/npm の配布状態
- `docs/real-repo-validation-evidence-20260704.md`: 4 repo 実行、1,721 findings、人手判定なし
- `orchestration/quality-evidence-os-implementation.md`: QEOS-031..042 の実装記録

## TDD / 検証

文書同期のみのため、新規テストは追加しない。親タスクで確認済みの関連 115 tests
と typecheck の結果を受入根拠として参照する。本タスクでは再試験しない。

親タスクの通常 full-suite baseline は 3,604 passed / 5 failed / 4 skipped
（timeout・性能上限超過を含む）であり、full-suite pass とは記録しない。

本タスクで実施する確認:

- `git diff --check`
- 変更文書から参照する主要ファイルの存在確認
- 変更対象が指定範囲に収まっていることの確認

## 完了条件

- 実装済みの3系統の precision 対策と Markdown ガイドが台帳に反映されている。
- `MISSING_INPUT_SANITIZATION`、real repo の human adjudication、npm 公開、外部 QEG 実行を残件として明示する。
- package/GitHub release `1.5.1` と npm 未公開の状態が文書間で矛盾しない。
- SPEC-MASTER が SPEC-30 と QEOS-031..042 の実装記録への導線を持つ。
- `docs/TASKS.md` が本 Task Seed へリンクする。
- 親レビュー完了後に `status: done` とし、残る人手精度判定や公開操作は別の残件として維持する。

## 検収観点

実装部分と文書同期は完了し、親レビューも完了した。今回の差分はコード、schema、CI、公開操作を
変更せず、40 文書の期限だけを一括更新しない。実 repo の 1,721 findings は
precision 合格とは扱わず、外部 QEG の実行結果も推測しない。

## Notes

- 基準 commit: `53897b9`、branch: `agent/security-and-evidence-fixes`
- `package.json` と最新 GitHub release は `1.5.1`。npm 状態は `docs/distribution-status.md` の記録に基づき、今回 API 再確認はしていない。
- commit / push / npm publish / GitHub 操作は実施しない。
