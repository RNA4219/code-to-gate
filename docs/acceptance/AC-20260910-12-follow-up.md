# 追加5項目の統合検収（2026-09-10）

状態: 追加5項目の実装・ローカル統合検証は完了。
実装: Luna workers。レビュー・統合検証: 親エージェント。
対象: `orchestration/follow-up-20260910.md`、Task 20260910-07〜11。
前回の5項目の検収は `AC-20260910-06-maintenance.md` に保持する。
後続のマージ前レビューと追加修正は [AC-20260910-13](AC-20260910-13-premerge-review.md) に記録する。

## 対象検証

| 段階 | 結果 |
|---|---|
| CI接続 | PR coverage / Release analyzeのbuild後へ接続。YAML構造確認成功。maintenance 13 tests成功 |
| 実行ID | runtime生成26ファイルを共通化、既存ID継承とagent IDを維持。cache-hit含む3 files / 10 tests、architecture 18 tests成功。その他対象検証はTask08に記録 |
| diff severity | 新規27 + 既存42 = 69 tests成功。省略可能な設定・inline YAML・DB差分・監査と終了判定の整合を含む |
| 精度レビュー画面 | 自立fixtureの対象テストと親UIイベント回帰14 tests成功。保存→既存集計→再読込、人手レビュー初期化、別run拒否、1,449件の29ページ移動と検索を実ブラウザで確認 |
| severity理由表示 | Reporter 2 files / 32 tests、Viewer 2 files / 62 tests成功。private/publicの表示差を実ブラウザで確認。Mermaid本文露出とブラウザのstyle直列化による件数誤表示も修正 |

## 最終ゲート

build / lint / typecheck / npm test / coverage（4指標80%）/ package smoke /
実ブラウザ操作 / Birdseye generate/check / docs lint・distribution・roadmap driftを検収対象とする。
ログ: `C:/Users/ryo-n/Codex_dev/code-to-gate-work-20260910-next/`。

- `npm test`: exit 0。通常Vitest 194 files / 3,710 passed + 既存4 skipped、tree-sitter 6 files / 63 passed、maintenance Node 13 passed。合計3,786 passed。
- `npm run build`: exit 0。package smoke内でdistを削除して再buildした。
- `npm run lint` / `npm run typecheck`: 最終表示修正後の再実行もexit 0。
- `npm run test:coverage`: 最終表示修正後もexit 0。113 files / 1,833 passed + 既存4 skipped。Statements 88.86%、Branches 80.73%、Functions 94.69%、Lines 89.84%。4指標すべて既存の80%閾値を満たす。
- `npm run test:package`: exit 0。1.6.0候補の615 filesをpackし、一時directoryへのinstall後にCLI/SDK、agent冪等再利用、analyze/viewer/diff、追加precision-review、空diffへのpolicy適用を検証した。tgzとpackage smoke用directoryはcleanup済み。
- 全体テスト後の表示調整は対象回帰、最終coverage、再buildしたpackage smoke、実ブラウザで確認した。EOF付近のコード省略表示とViewerの絞り込み件数も一致した。
- ブラウザ操作の詳細と保存JSONはログdirectoryの `browser-qa.md` / `ui-artifacts/browser-saved-review.json` / `ui-artifacts/browser-summary.json` に保存した。検証用serverとタブは停止した。
- `node scripts/birdseye.mjs generate` / `check`: exit 0。最終文書とsourceから112 nodesを再生成し、整合確認が成功した。
- `npm run docs:lint-refs` / `npm run docs:distribution`: exit 0。`node scripts/check-roadmap-completion-drift.mjs --fail`もexit 0、drift候補0件。
- `git diff --check`: exit 0。既存の改行形式に関するwarningのみで、差分の空白エラーはない。

package smokeの最初の試行はsandbox内のnpm取得がEACCESで進まなかったため中断した。
必要なレジストリ通信を許可した再実行が成功した。パッケージ公開は行っていない。

## 境界

- GitHub hosted runnerのCI実行は未実施。ローカルの同等コマンドとworkflow構造を検証する。
- UI回帰テストの判定は合成fixtureの操作であり、実repoの人手精度評価として記録しない。
- 過去のApp/PAT採用決定とキックオフ開催の記録は未確認のまま。
- このローカル検収時点ではcommit、push、merge、tag、GitHub Release、npm publishは未実施。後続のPR統合とパッケージ公開は別に追跡する。
