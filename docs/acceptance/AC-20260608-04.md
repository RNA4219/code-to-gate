---
acceptance_id: AC-20260608-04
task_id: 20260608-04
intent_id: INT-ASSURANCE-SMELL-DETECTOR-001
owner: code-to-gate-team
status: approved
reviewed_at: 2026-06-09
reviewed_by: Claude
approval_type: technical
release_approval_id: null
---

# Acceptance Record: Wave 1 Task 04 - AssuranceGraph and Input Normalization

## Scope

- 対象変更:
  - AssuranceGraph型定義（nodes, edges, coverage）
  - typed artifact bundleからの純粋なgraph正規化
  - Node正規化: finding, risk, invariant, test-seed, evidence, file, symbol, entrypoint, readiness-condition
  - Edge正規化: supported-by, affects, declares, maps-to, derived-from
  - Coverage tracking: loaded, missing, partial flags
- 非対象:
  - detection rules
  - unsupported_claims generation (Task 03 scope)
  - CLI、artifact loader/writer、JSON/YAML parse、schema validation（Task 10）

## Acceptance Criteria

- [x] AssuranceGraph型定義（nodes, edges, coverage）
- [x] Node正規化（finding, risk, invariant, test-seed, readiness-condition, file, symbol, entrypoint, evidence）
- [x] Edge正規化（sourceFindingIds, sourceRiskIds, evidence refs）
- [x] Coverage tracking（loaded, missing, partial flags）
- [x] 空bundle時はcoverage.missingに記録、graphは空nodes/edges
- [x] valid bundle時はnodes/edges正規化成功
- [x] typecheck成功
- [x] architecture boundary compliance

## Evidence

- 対象テスト: `src/application/assurance/__tests__/assurance-graph.test.ts`
- 結果:
  - graph tests: 25 passed
  - architecture: 18 passed
  - lint: 0 warnings
  - typecheck: 成功
  - full suite: `npm test`成功（約312秒）

## Technical Details

### Type Alignment

RepoSymbol/RepoEntrypoint local interfaces:
- NormalizedRepoGraph.symbols/entrypoints は `unknown[]`
- local interfaceでcastしてnode/edge生成

### Boundary Correction

- 初回実装に含まれていた`node:fs`、`node:path`、簡易YAML parserをapplication層から除去
- `buildAssuranceGraph()`はtyped `AssuranceArtifactBundle`だけを受け取る純粋関数へ変更
- application層のNode API直接importをESLintとarchitecture testで禁止

## Verification Result

- 判定: approved
- コメント: AssuranceGraph正規化完了。typed bundle入力へ純粋化し、application層のNode API直接importなしを自動検証する。
- フォローアップ: Wave 1 Task 05（EVIDENCE_MISSING detection rule）

## Known Debt

- RiskSeed.mitigationStatus, TestSeed.priority等はschemaに存在しないため、node dataには含まない
- RepoSymbol/RepoEntrypoint interfaceはtypes/artifacts.tsのunknown[]定義に合わせたlocal cast
- artifact loaderとJSON/YAML parseはTask 20260608-10へ繰延べ
- requirement/intent node正規化はintake契約を実装するTask 20260608-07へ繰延べ
