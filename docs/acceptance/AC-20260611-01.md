---
acceptance_id: AC-20260611-01
task_id: 20260611-10
intent_id: INT-SQL-DATABASE-ANALYSIS-001
owner: code-to-gate-team
status: approved
reviewed_at: 2026-06-11
reviewed_by: codex
approval_type: technical
release_approval_id: null
---

# SQL・データベース変更リスク解析 Acceptance Record

## Scope

- 対象変更: `SPEC-29`、`database-assets@v1alpha1`、SQL軽量解析、DB rule、scan/analyze/diff配線、schema検証、fixture・テスト・公開ドキュメント
- 非対象: 実DB接続、ORM固有migration完全解析、migration実行、release decision

## Acceptance Criteria

- [x] `--database-analysis`指定時だけDB解析とDB ruleを有効化する
- [x] `database-assets.json`をoptional artifactとして生成・検証する
- [x] 既存`normalized-repo-graph@v1`契約へ`sql` languageや`migration` roleを追加しない
- [x] 8 DB ruleのpositive/negative/refutation testを通す
- [x] scan/analyze/diffへ明示フラグを配線する
- [x] base/head間で新たに導入されたDBリスクだけをdiff findingとして出力する
- [x] 未知方言・部分解析失敗をdiagnostic化し、解析済み結果を保持する
- [x] DB findingをrisk、test seed、SARIF、readiness入力へ追跡できる
- [x] credential候補をdatabase artifactへ出力しない
- [x] SQL合計10 MiB・1000ファイルを30秒以内で解析する
- [x] 仕様・実装計画・Task Seed・台帳・公開ドキュメントを整合させる

## Evidence

- 実行コマンド: `npm run lint`
- 実行コマンド: `npm run typecheck`
- 実行コマンド: `npm run build`
- 実行コマンド: `npm run test:smoke`
- 実行コマンド: `npm run test:architecture`
- 実行コマンド: `npm run test:package`
- 実行コマンド: `code-to-gate analyze`のフラグなし・フラグあり比較と`schema validate-all --allow-missing`
- 実行コマンド: 実Git履歴を用いた`diff --database-analysis --base base --head head`
- 実行コマンド: 1000 SQLファイル・10.30 MiBのdatabase analyzer性能計測
- 実行コマンド: `npx vitest run src/adapters/__tests__/sql-lightweight-parser.test.ts src/rules/__tests__/db-destructive-ops.test.ts src/rules/__tests__/db-schema-change.test.ts src/rules/__tests__/db-migration-ops.test.ts tests/integration/demo-migrations-ts.test.ts tests/integration/database-edge-cases.test.ts tests/integration/database-diff.test.ts src/reporters/__tests__/yaml-reporter.test.ts --maxWorkers=1 --reporter=dot`
- 実行コマンド: `npx vitest run tests/integration/database-performance.test.ts --maxWorkers=1 --reporter=dot`
- テスト結果: 対象unit/integration・YAML回帰 158件、性能回帰 2件、smoke 54件、architecture 18件、package smokeが成功
- CLI検証結果: フラグなしはDB artifact/findingsなし、フラグありはDB artifactと32 findingsを生成し、全artifactがschema-valid
- 差分検証結果: base既存のDROP TABLEを除外し、headで追加したDROP TABLEだけをfinding化
- 安全性検証結果: 未知方言warning、部分解析`partial`、秘密値マスキング、SQLなし完全成功を確認
- artifact追跡結果: DB findingからrisk、test seed、SARIF、readiness入力まで追跡可能
- 性能計測結果: 1000ファイル、10.30 MiBを4.102秒で解析。性能回帰テストでは1000ファイルを1.982秒、analyze CLIを3.253秒で完了
- 参照ドキュメント: `docs/specs/SPEC-29-sql-database-analysis.md`
- 参照ドキュメント: `docs/implementation-plan-sql-database-analysis.md`
- 参照ドキュメント: `docs/task-seeds/SPEC-29-ledger.md`

## Verification Result

- 判定: approved
- コメント: DB解析は明示フラグ時のみ有効で、stable graph契約を変更せずにoptional artifactとDB findingsを生成する。base/headの新規DBリスク差分、diagnostic、秘密値マスキング、既存artifact追跡、性能目標を検証済み。
- フォローアップ: 全体`npm test`は`test:package`との並列実行で`dist`削除競合が発生したため、リリース時は順次実行する。
- 既知gap: `schema validate-all --strict`はanalyzeが任意生成する`release-readiness.json`不在を必須artifact欠落として扱う。SPEC-29 artifact群は`--allow-missing`で全件schema-validを確認済み。

## Release Mapping

- Release Approval ID:
- Release Version:
- Release Note:
